import "Windows.Media.idl"

#include <sdkddkver.h>

namespace GrayscaleTransform
{
   [version(NTDDI_WIN8), activable(NTDDI_WIN8)]
   runtimeclass GrayscaleEffect
   {
      [default] interface Windows.Media.IMediaExtension;
   }
}
